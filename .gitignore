# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor lock files (LibreOffice, etc.)
.~lock.*
*.swp
*.swo
.idea/
.vscode/

# Working files / private submission artifacts (kept local, not committed)
# Tracked screenshots live in screenshots/ — keep root copies out of git
# so accidental `git add -A` later doesn't duplicate them.
# STORE_LISTING.md moved to docs/store-submission.md and is TRACKED now:
# it holds no secrets (the text is what goes on a public store page), and
# ignored means it dies with this machine. 2026-08-23.
icon-options/
Demo_image_interface.png
Demo_image_save_file.png
Demo_image_interface_1280x800.png
Demo_image_save_file_1280x800.png

# Build output
*.pyc
__pycache__/
node_modules/

# Local notes
NOTES.md
TODO.md

# v1.5 test artifacts
tests/out-sample.zip
