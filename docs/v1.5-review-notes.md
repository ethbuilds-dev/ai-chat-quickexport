# v1.5 media export — reviewer desk-pass (code-eth, 2026-07-07 evening)

Scope: full desk review of branch v1.5-media-export (5 commits). Browser
phase NOT yet run — see "remaining" below.

## Verified by the reviewer's own hands
- Test suite re-run independently: 66/66 pass. `node --check` green on all
  four shipped JS files. Sample zip re-validated.
- Manifest: x.com permission intact (diff artifact, not a removal);
  oaiusercontent.com addition is correct and justified for ChatGPT assets.
- popup.js: zero deleted lines (insertions-only claim TRUE).
- background.js deletions inspected line-by-line: the necessary walk
  changes (parts handling for media pointers), covered by fixtures.

## Reviewer false alarm, disproven by deeper read (kept for honesty)
Suspected dangling `](asset:id)` links in plain exports of image-bearing
conversations. WRONG: media.length>0 always routes through doMediaExport,
which rewrites every placeholder — success → relative path, failure →
italic "not exported" note; both fallback paths (no assets, zip failure)
still ship the conversation text. No dangling links possible. The
architecture is better than the objection.

## Design observations (accept, but document in release notes)
1. Image-bearing conversations now ALWAYS export as .zip — no user toggle.
   Correct default for v1.5's purpose; must be stated in release notes.
2. Claude web fields (files/attachments) and the entire ChatGPT asset side
   remain INFERRED (agent's G1–G7 table) — implemented log-and-skip, so
   worst case is a clean skip, not a break.

## Remaining before this touches Zaina (the streak's verified bar)
Browser phase, per docs/v1.5-MANUAL-TEST-PLAN.md: (1) byte-identical
regression on text-only exports, all four platforms; (2) one real Claude
image export (settles G1–G3); (3) one real ChatGPT image export (G4–G6).
Needs ~5 min of human hands once (load unpacked extension), then the tests
can be driven via browser automation. Version bump + release ONLY after
all three pass.

Desk verdict: READY FOR BROWSER PHASE.
